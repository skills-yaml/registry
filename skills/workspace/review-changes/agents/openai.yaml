interface:
  display_name: "Review Changes"
  short_description: "Review changes for defects and regressions"
  default_prompt: "Use $review-changes to review this working tree, branch, commit range, or pull request."
