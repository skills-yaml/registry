interface:
  display_name: "Fix CI"
  short_description: "Diagnose and fix failing CI checks"
  default_prompt: "Use $fix-ci to diagnose the failing checks, implement the authorized fix, and revalidate."
