interface:
  display_name: "wk.plan"
  short_description: "Validate a spec and plan implementation"
  default_prompt: "Use $wk-plan to validate or create the active spec and produce an executable implementation plan."
policy:
  allow_implicit_invocation: false
