interface:
  display_name: "Fix Bug"
  short_description: "Reproduce and fix a reported defect"
  default_prompt: "Use $fix-bug to reproduce this defect, implement the smallest fix, and add regression coverage."
