# Workspace Docs Standard

registry adopts `workspace-docs@1.0.0`.

The generated context in `AGENTS.md` is the repository-local adoption pin. This
file intentionally records the pin without embedding machine-local or external
workspace locations.
